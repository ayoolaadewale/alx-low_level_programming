#include <stdlib.h>
#include <stdio.h>
#include <time.h>

/**
 *main - Entry point
 *Return: Zero at the end
 */

int main(void)
{
	int n;

	srand(time(0);
	n = rand() - RAND_MAX / 2;
	for (int i = 'A'; i <= 'Z'; i++)
	{
		putchar(tolower(i));
	}
	return (0);
}
