#include "main.h"
#include <stdio.h>

/**
 *_strlen - Entry point
 *@s: Parameter
 *Return: Length of array
 */

int _strlen(char *s)
{
	int array_length;

	array_length = strlen(s);
	return (array_length);
}
